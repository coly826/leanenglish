<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>E-Learning English - Espace Utilisateur</title>

  <!-- Liens CSS externes -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    /* ====== STYLE GLOBAL ====== */
    body {
      background: linear-gradient(135deg, #fff8f0, #ffe8cc);
      font-family: 'Poppins', sans-serif;
      color: #333;
      overflow-x: hidden;
    }

    /* ====== NAVBAR ====== */
    .navbar {
      background: linear-gradient(90deg, #ff7b00, #ffae42);
      box-shadow: 0 4px 15px rgba(0,0,0,0.15);
      animation: fadeDown 0.8s ease;
    }

    .navbar-brand {
      font-weight: 700;
      font-size: 1.4rem;
    }

    .nav-link {
      color: #fff !important;
      font-weight: 600;
      transition: 0.3s;
    }

    .nav-link:hover {
      color: #ffe8cc !important;
      transform: translateY(-2px);
    }

    /* ====== TITRE PRINCIPAL ====== */
    h3 {
      color: #ff7b00;
      font-weight: 700;
      margin-top: 100px;
      animation: fadeIn 1s ease-in-out;
    }

    /* ====== CARDS ====== */
    .card {
      border: none;
      border-radius: 20px;
      background: white;
      box-shadow: 0 6px 20px rgba(255, 123, 0, 0.15);
      transition: all 0.4s ease;
      overflow: hidden;
      position: relative;
      transform: scale(1);
      animation: popUp 0.8s ease-in-out;
    }

    .card:hover {
      transform: translateY(-10px) scale(1.03);
      box-shadow: 0 10px 25px rgba(255, 123, 0, 0.25);
    }

    .card::before {
      content: "";
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 5px;
      background: linear-gradient(90deg, #ff7b00, #ffae42);
      transition: 0.5s;
    }

    .card:hover::before {
      left: 0;
    }

    .card h5 {
      color: #ff7b00;
      font-weight: 700;
      margin-bottom: 10px;
    }

    .card p {
      font-size: 0.95rem;
      color: #555;
    }

    .btn-info {
      background-color: #ff7b00;
      border: none;
      border-radius: 30px;
      font-weight: 600;
      padding: 8px 16px;
      transition: all 0.3s ease;
    }

    .btn-info:hover {
      background-color: #ff9933;
      transform: scale(1.05);
    }

    /* ====== FOOTER ====== */
    footer {
      margin-top: 60px;
      text-align: center;
      color: #777;
      font-size: 0.9rem;
      padding: 20px 0;
      animation: fadeUp 1s ease;
    }

    /* ====== ANIMATIONS ====== */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes popUp {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }

    /* ====== RESPONSIVE ====== */
    @media (max-width: 768px) {
      h3 { font-size: 1.4rem; }
      .btn-info { font-size: 0.9rem; padding: 6px 14px; }
    }

  </style>
</head>

<body>

  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/" style="font-size: 19px;">
        <i class="bi bi-mortarboard-fill me-2 "></i> Welcome to Hello Time
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a href="/exercises" class="nav-link active"><i class="bi bi-pencil-square"></i> Tester vos compétences</a></li>
          <li class="nav-item"><a href="/admin" class="nav-link"><i class="bi bi-person-gear"></i> Admin</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- CONTENU -->
  <div class="container mt-5">

    <h3 class=" mb-5"><i class="bi bi-person-circle me-2"></i>With Mr DIOUF Abiboulaye</h3>
    <h3 class="text-center mb-4"><i class="bi bi-person-circle me-2"></i>Espace Utilisateur</h3>

    <div class="row mt-4">
      <% if (contents.length === 0) { %>
        <p class="text-center text-muted">Aucun contenu disponible pour le moment.</p>
      <% } %>

      <% contents.forEach(content => { %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card p-4">
            <h5><%= content.title %></h5>
            <p><%= content.fileName %> - <%= content.type %></p>
            <a href="<%= content.filePath %>" target="_blank" class="btn btn-info btn-sm">
              <i class="bi bi-eye me-1"></i> Voir le contenu
            </a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <h3 class="text-center mb-4">Reading 📘</h3>
<div class="row mb-5">
  <% contents.filter(c => c.skill === 'Reading').forEach(content => { %>
    <div class="col-md-6 col-lg-4 mb-4">
      <div class="card p-4">
        <h5><%= content.title %></h5>
        <p><%= content.fileName %> - <%= content.type %></p>
        <a href="<%= content.filePath %>" target="_blank" class="btn btn-info btn-sm">
          <i class="bi bi-eye me-1"></i> Voir le contenu
        </a>
      </div>
    </div>
  <% }) %>
</div>

<h3 class="text-center mb-4">Writing ✍️</h3>
<!-- Même structure pour Writing, Listening, Speaking -->


  <!-- FOOTER -->
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Welcome to Hello Time</p>
  </footer>

  <!-- SCRIPTS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
