<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>E-Learning English - Gestionnaire Prestige</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container">
      <a class="navbar-brand" href="#">
        <i class="bi bi-mortarboard-fill me-2"></i> E-Learning English PRO
      </a>
    </div>
  </nav>

  <div class="container">
    <h3 class="mb-4 text-primary">Espace Administrateur</h3>

    <form action="/upload" method="POST" enctype="multipart/form-data" class="row g-3">
      <div class="col-md-3">
        <select name="contentType" class="form-select">
          <option value="PDF">Cours PDF</option>
          <option value="Video">Cours Vidéo</option>
          <option value="Exercise">Exercice</option>
        </select>
      </div>
      <div class="col-md-5">
        <input type="text" name="contentTitle" class="form-control" placeholder="Titre du contenu">
      </div>
      <div class="col-md-4">
        <input type="file" name="contentFile" class="form-control">
      </div>
      <div class="col-12 text-end mt-3">
        <button class="btn btn-success" type="submit"><i class="bi bi-upload"></i> Ajouter</button>
      </div>
    </form>

    <hr class="my-4">

    <h4>Contenus enregistrés</h4>
    <div class="row">
      <% if (contents.length === 0) { %>
        <p class="text-center text-muted">Aucun contenu ajouté.</p>
      <% } %>

      <% contents.forEach(content => { %>
        <div class="col-md-6 mb-4">
          <div class="card p-3 shadow-sm">
            <h5><%= content.title %></h5>
            <p><%= content.fileName %> - <%= content.type %></p>
            <div class="d-flex justify-content-between">
              <a href="<%= content.filePath %>" target="_blank" class="btn btn-info btn-sm">Voir</a>

              <form action="/toggle/<%= content.id %>" method="POST" style="display:inline;">
                <button class="btn <%= content.shared ? 'btn-success' : 'btn-secondary' %> btn-sm">
                  <%= content.shared ? 'Partagé' : 'Privé' %>
                </button>
              </form>

              <form action="/delete/<%= content.id %>" method="POST" style="display:inline;">
                <button class="btn btn-danger btn-sm">Suppr.</button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
