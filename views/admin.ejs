<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>E-Learning English - Espace Administrateur</title>
  
  <!-- Bootstrap + Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #fff8f0, #ffe8cc);
      font-family: 'Poppins', sans-serif;
      color: #333;
      overflow-x: hidden;
    }

    /* --- NAVBAR --- */
    .navbar {
      background: linear-gradient(90deg, #ff7b00, #ffae42);
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      animation: slideDown 0.8s ease;
    }

    @keyframes slideDown {
      from { transform: translateY(-50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }

    .navbar-brand {
      font-weight: 700;
      font-size: 1.4rem;
    }

    .nav-link {
      color: white !important;
      transition: 0.3s;
      font-weight: 600;
    }

    .nav-link:hover {
      color: #fff3e0 !important;
      transform: scale(1.05);
    }

    /* --- TITRES --- */
    h3, h4, h5 {
      color: #ff7b00;
      font-weight: 700;
      animation: fadeIn 1s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* --- FORMULAIRE --- */
    form {
      border-radius: 15px;
      background: white;
      box-shadow: 0 4px 20px rgba(255, 123, 0, 0.15);
      padding: 25px;
      transition: all 0.3s ease;
      animation: fadeInUp 1s ease;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .form-control, .form-select {
      border-radius: 10px;
      border: 1px solid #ffc27a;
      box-shadow: none;
      transition: border-color 0.3s;
    }

    .form-control:focus, .form-select:focus {
      border-color: #ff7b00;
      box-shadow: 0 0 5px rgba(255, 123, 0, 0.4);
    }

    .btn-success {
      background-color: #ff7b00;
      border: none;
      border-radius: 30px;
      font-weight: 600;
      transition: 0.3s;
      box-shadow: 0 4px 10px rgba(255, 123, 0, 0.3);
    }

    .btn-success:hover {
      background-color: #ff9933;
      transform: scale(1.07);
      box-shadow: 0 6px 15px rgba(255, 123, 0, 0.4);
    }

    /* --- CARTES DE CONTENU --- */
    .card {
      border: none;
      border-radius: 15px;
      background: white;
      box-shadow: 0 4px 10px rgba(255, 123, 0, 0.15);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      perspective: 1000px;
    }

    .card:hover {
      transform: translateY(-10px) rotateX(3deg);
      box-shadow: 0 10px 25px rgba(255, 123, 0, 0.3);
    }

    .card h5 {
      color: #ff7b00;
      font-weight: 700;
    }

    .btn-warning {
      background-color: #ffae42;
      border: none;
      transition: 0.3s;
    }

    .btn-warning:hover {
      background-color: #ff9100;
      transform: scale(1.05);
    }

    .btn-danger:hover {
      transform: scale(1.05);
    }

    /* --- FOOTER --- */
    footer {
      margin-top: 60px;
      text-align: center;
      color: #777;
      font-size: 0.9rem;
      padding: 20px 0;
      animation: fadeIn 1.5s ease;
    }

    /* --- RESPONSIVE --- */
    @media (max-width: 768px) {
      .navbar-brand { font-size: 1.2rem; }
      h3 { font-size: 1.3rem; }
      .btn { font-size: 0.85rem; }
    }

    @media (max-width: 576px) {
      .form-control, .form-select { font-size: 0.9rem; }
      .card { padding: 15px; }
    }
  </style>
</head>

<body>
  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg navbar-dark mb-4 fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/admin">
        <i class="bi bi-mortarboard-fill me-2"></i> Welcome to Hello Time
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a href="/" class="nav-link"><i class="bi bi-person"></i> Côté utilisateur</a></li>
          <li class="nav-item"><a href="/exercises" class="nav-link active"><i class="bi bi-pencil-square"></i> Tester vos compétences</a></li>

          <li class="nav-item"><a href="/logout" class="nav-link text-warning"><i class="bi bi-box-arrow-right"></i> Déconnexion</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- CONTENU -->
  <div class="container mt-5 pt-5">
    <h3 class=" mb-5"><i class="bi bi-person-circle me-2"></i>With Mr DIOUF Abiboulaye</h3>

    <h3 class="mb-4 text-center"><i class="bi bi-gear-fill me-2"></i>Espace Administrateur</h3>

    <form action="/upload" method="POST" enctype="multipart/form-data" class="row g-3 mb-5">
      <div class="col-md-3 col-sm-12">
        <select name="contentType" class="form-select" required>
          <option value="">-- Type de contenu --</option>
          <option value="PDF">Cours PDF</option>
          <option value="Video">Cours Vidéo</option>
          <option value="Exercise">Exercice</option>
        </select>
      </div>
    
      <!-- 🆕 AJOUT : Choix de compétence -->
      <div class="col-md-3 col-sm-12">
        <select name="skillType" class="form-select" required>
          <option value="">-- Choisir compétence --</option>
          <option value="Reading">📘 Reading</option>
          <option value="Writing">✍️ Writing</option>
          <option value="Listening">🎧 Listening</option>
          <option value="Speaking">🗣️ Speaking</option>
        </select>
      </div>
    
      <div class="col-md-3 col-sm-12">
        <input type="text" name="contentTitle" class="form-control" placeholder="Titre du contenu" required>
      </div>
    
      <div class="col-md-3 col-sm-12">
        <input type="file" name="contentFile" class="form-control" required>
      </div>
    
      <div class="col-12 text-end mt-3">
        <button class="btn btn-success" type="submit"><i class="bi bi-upload"></i> Ajouter</button>
      </div>
    </form>
    

    <h4 class="mb-3"><i class="bi bi-collection me-2"></i>Contenus enregistrés</h4>

    <div class="row mt-3">
      <% if (contents.length === 0) { %>
        <p class="text-center text-muted">Aucun contenu ajouté.</p>
      <% } %>

      <% contents.forEach(content => { %>
        <div class="col-lg-6 col-md-6 col-sm-12 mb-4">
          <div class="card p-4 shadow-sm">
            <h5><%= content.title %></h5>
            <p><%= content.fileName %> - <%= content.type %></p>
            <div class="d-flex flex-wrap gap-2 justify-content-between">
              <a href="<%= content.filePath %>" target="_blank" class="btn btn-warning btn-sm text-white">
                <i class="bi bi-eye"></i> Voir
              </a>

              <form action="/toggle/<%= content.id %>" method="POST" class="d-inline">
                <button class="btn <%= content.shared ? 'btn-success' : 'btn-secondary' %> btn-sm">
                  <i class="bi bi-share"></i> <%= content.shared ? 'Partagé' : 'Privé' %>
                </button>
              </form>

              <form action="/delete/<%= content.id %>" method="POST" class="d-inline">
                <button class="btn btn-danger btn-sm">
                  <i class="bi bi-trash"></i> Supprimer
                </button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>


  
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> E-Learning English PRO - Administration</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
